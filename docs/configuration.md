# 箱入り娘の設定
箱入り娘の設定ファイルは、LINEボットサーバーの設定ファイル`application.yml`と、センサーサーバーの設定ファイル`config.toml`に分かれており、
それぞれYAML形式とTOML形式で記述されています。

## LINEボットサーバーの設定（`application.yml`）
`application.yml`では、Spring Bootアプリケーションとして動作するLINEボットサーバーの設定を行います。
